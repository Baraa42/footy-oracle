{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --parallel",
    "serve": "lerna run serve --parallel",
    "cloud-watch": "lerna cloud-watch --parallel",
    "deploy": "lerna deploy --parallel",
    "hardhat": "cd ./packages/hardhat && npx hardhat",
    "coverage": "cd ./packages/hardhat && npx hardhat coverage"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
